import React from 'react'
import styled from 'styled-components'

const Wrapper = styled.div`
  background-color: var(--highlight-pink);
  height: 30rem;
  width: 100vw;
  display: flex;
  align-items: center;
`

function Pricing() {
  return <Wrapper>Pricing..</Wrapper>
}

export default Pricing
